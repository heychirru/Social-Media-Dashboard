package com.example.dashboard.utils;

public class ApiUtils {

    public static String buildApiUrl(String baseUrl, String endpoint) {
        return baseUrl + endpoint;
    }

    public static String handleApiResponse(String response) {
        // Process the API response and return the relevant data
        return response; // Placeholder for actual response handling logic
    }
}